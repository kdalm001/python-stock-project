🚀 Python Stock Price Tracker

A simple command-line (CLI) application that tracks the simulated real-time prices of a curated list of high-performing stocks. This project was built to demonstrate proficiency in Python, API data fetching, and data handling with pandas.

📋 Features

Real-Time Data: Fetches the latest market price and daily change every 10 seconds.

Top Performer List: Tracks a pre-defined list of 10 high-performing stocks (e.g., NVDA, TSLA, MSFT).

Clean UI: Displays the data in a clean, easy-to-read table directly in your terminal.

Dynamic Sorting: Sorts the list to show the day's biggest gainers at the top.

🛠️ Technologies Used

Python 3

yfinance: The library used to fetch stock data from Yahoo! Finance.

pandas: The library used to organize and display the data in a clean, sorted table.

🏃 How to Run This Project

1. Clone the repository:

git clone [https://github.com/kdalm001/python-stock-project.git](https://github.com/kdalm001/python-stock-project.git)
cd python-stock-project


2. Install the required libraries:
Make sure you have pip (Python's package installer) installed.

pip install -r requirements.txt


3. Run the application:

python3 stock_tracker.py


4. Stop the application:
Press Control + C in your terminal.
